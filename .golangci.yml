version: "2"
run:
  timeout: 5m

formatters:
  enable:
    - gofmt
    - goimports

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - misspell
    - revive
    - staticcheck
    - unused
    - errorlint
    - gocritic
    - nilnil
    - bodyclose

  settings:
    gocritic:
      disabled-tags:
        - performance
    revive:
      rules:
        - name: exported
          disabled: true
    errcheck:
      check-type-assertions: true

  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
          - gocritic

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
